# ============================================
# Vibe Coding Toolkit - Git 属性配置
# 作用：统一跨平台换行符，防止脚本执行出错
# ============================================

# 默认行为：自动检测文本文件，并设置换行符为 LF（适合 bash 脚本）
* text=auto eol=lf

# 强制所有 Shell 脚本使用 LF（这是你的核心文件，必须锁定）
*.sh text eol=lf

# 强制所有 Markdown 文档使用 LF
*.md text eol=lf

# 强制所有 YAML 配置文件使用 LF（SKILL.md 的前置元数据）
*.yml text eol=lf
*.yaml text eol=lf

# 强制 JSON 文件使用 LF
*.json text eol=lf

# Windows 特有的批处理文件，强制使用 CRLF（以防万一你以后用到）
*.bat text eol=crlf
*.cmd text eol=crlf

# ============================================
# 二进制文件声明（防止 Git 尝试转换或自动合并）
# ============================================
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.zip binary
*.tar.gz binary